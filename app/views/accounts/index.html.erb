<div class="container">
  <div class="row">
    <h3>Client Accounts <small>(<%= Account.count %> active)</small></h3>
    <div class="column">
      <table class="table">
        <tbody>
          <% @accounts.each do |account| %>
            <tr>
              account
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>